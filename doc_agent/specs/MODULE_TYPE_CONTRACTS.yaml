module_types:
  - id: common
    name: Common Utilities
    contract:
      inputs: ["function calls"]
      outputs: ["utility functions", "middleware"]
      notes: "No business logic; English documentation required."
  - id: example
    name: Example Module
    contract:
      inputs: ["HTTP request"]
      outputs: ["HTTP response"]
      notes: "Reference only."

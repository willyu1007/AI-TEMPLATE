# Test Plan - Common Module

Demonstration module for shared utilities. Tests live under `tests/common/`.

- Unit tests: pytest for utils/models, Go/TS examples as needed.
- Integration tests: scripts that exercise middleware and interfaces.
- Coverage target: ¡Ý90%.
- Language: English per `config/language.yaml`.

Update when adding new helpers.

